object FrontendCommands {

  val dependencyInstall: String = "yarn install"
  val test: String = "yarn run test:none"
  val serve: String = "yarn run start"
  val build: String = "yarn run build:prod"
}
